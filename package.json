{
  "scripts": {
    "deploy:to": "npx hardhat compile && npx hardhat run scripts/deploy.js --network "
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.0",
    "hardhat": "^2.12.2"
  },
  "dependencies": {
    "dotenv": "^16.0.3"
  }
}